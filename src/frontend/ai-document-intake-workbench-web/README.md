# AI Document Intake Workbench Web

Minimal Angular application shell for PR01.

```bash
npm install
npm run build
npm start
```

The local development server uses `http://localhost:4200/` by default.
